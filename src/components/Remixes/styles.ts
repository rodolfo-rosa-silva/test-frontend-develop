const styles = {
  container: {
    display: 'flex',
    flexDirection: 'column',
    justifyContent: 'center',
    alignItems: 'center',

    table: {
      width: '1200px',
      fontSize: '16px',
      textAlign: 'left'
    }
  }
};

export default styles;
